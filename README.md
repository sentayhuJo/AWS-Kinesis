# AWS-Kinesis

## INITIAL SETUP 
    - AWS credential should be configured in the local machine 
            and 
    `$ npm install` 

## TO PRODUCE DATA
    `$ node produce`

## TO CONSUME DATA
    `$ node get_record`
